import 'package:flutter/material.dart';

class CampaignBoostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'CampaignBoost Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
